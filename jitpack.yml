jdk:
  - openjdk17

before_install:
  - chmod +x gradlew
  - sdk install java 17.0.12-oracle || true
  - sdk use java 17.0.12-oracle || true

install:
  - ./gradlew clean build publishToMavenLocal


cache:
  directories:
    - $HOME/.gradle/caches
    - $HOME/.gradle/wrapper